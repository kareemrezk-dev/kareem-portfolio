/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: '**',
      },
    ],
  },
  async rewrites() {
    return [
      {
        source: '/pricing',
        destination: '/pricing.html',
      },
    ]
  },
}
module.exports = nextConfig
