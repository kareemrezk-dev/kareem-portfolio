export const metadata = {
  title: 'Admin Dashboard — Kareem Rezk',
  description: 'Admin dashboard for managing the portfolio.',
}

export default function AdminLayout({ children }) {
  return children
}
